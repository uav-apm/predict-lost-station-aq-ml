# Shared baseline for Delhi Extra Trees PM2.5-only experiments.

extends: delhi_extra_trees_base.yaml

data:
  target_pollutant: "PM2.5 (µg/m³)"

model:
  algorithm_name: Extra Trees (PM2.5)
  target: "PM2.5 (µg/m³)"
